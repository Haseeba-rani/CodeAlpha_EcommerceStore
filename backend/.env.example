# Server
NODE_ENV=development
PORT=5000

# Frontend origin (for CORS)
CLIENT_URL=http://localhost:3000

# Firebase — EITHER paste your service account key JSON as a single-line
# string below, OR leave this blank and place the downloaded key file at
# backend/config/serviceAccountKey.json instead (see README.md).
FIREBASE_SERVICE_ACCOUNT=
